interface:
  display_name: "Park"
  short_description: "Capture useful context without expanding current scope"
  default_prompt: "Use $park to save this idea for later without derailing the current work."
policy:
  allow_implicit_invocation: true
