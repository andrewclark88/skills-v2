interface:
  display_name: "Research"
  short_description: "Produce grounded research from fetched source evidence"
  default_prompt: "Use $research to investigate this question and produce a grounded research brief."
policy:
  allow_implicit_invocation: true
