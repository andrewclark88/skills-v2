interface:
  display_name: "Workbench Setup"
  short_description: "Consolidate any repository into clean Workbench state"
  default_prompt: "Use $setup to consolidate this repository into a clean Workbench setup."
policy:
  allow_implicit_invocation: true
