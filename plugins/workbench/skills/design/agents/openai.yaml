interface:
  display_name: "Design"
  short_description: "Shape and review implementation-ready designs"
  default_prompt: "Use $design to shape and review this work before implementation."
policy:
  allow_implicit_invocation: true
