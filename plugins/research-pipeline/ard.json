{
  "_comment": "Consumer-side ARD pin + vendor map for research-pipeline — the SINGLE source of truth for which ARD release this plugin's verification adapter adopts and where each upstream kernel artifact is vendored. Mirrors the upstream agentic-research ard.json (https://code.s-nc.org/Kevoun/ARD). The plugin's OWN semver (.claude-plugin/.codex-plugin/package.json `version`) is DECOUPLED: bump the plugin when the plugin changes; bump `adopts` only on an ARD version sync. Re-sync = re-copy the vendored_paths verbatim from the pinned ARD release, then run conformance. See docs/ard-adoption-plan.md.",
  "framework": "ARD",
  "homepage": "https://code.s-nc.org/Kevoun/ARD",
  "adopts": {
    "version": "0.4.1",
    "release_tag": "v0.4.1",
    "commit_sha": "abbcfcdee9d9682b60c2eac99519ccdb8ea359b0",
    "catalog_baseline": "0.3",
    "discipline_sha256": "2cd737c2e084ba2795df1a0542139d9b92d5a444382c1dfdfb1f4d4fd5b03b6d",
    "_discipline_sha256_note": "sha256 of the SIX numbered sections (from the '## 1. Source-bound' heading to EOF) of skills/research-discipline/SKILL.md — the verbatim ARD kernel/discipline.md body, EXCLUDING our deployment wrapper above it. test_discipline_verbatim.py enforces it so a stray edit or re-sync can't silently drift the anti-fabrication floor. Regenerate on an intentional ARD bump with: awk '/^## 1\\. Source-bound/{p=1} p' skills/research-discipline/SKILL.md | shasum -a 256"
  },
  "vendored_from": {
    "_comment": "Vendored verbatim from nklisch/skills plugins/agentic-research (which vendors ARD's kernel). pin, don't fork — invoke via flags, never patch.",
    "upstream_plugin": "agentic-research"
  },
  "vendored_paths": {
    "kernel/lint-citations.py": "scripts/lint-citations.py",
    "kernel/catalogs.json": "scripts/catalogs.json",
    "kernel/schema/attestation.schema.json": "scripts/schema/attestation.schema.json",
    "kernel/conformance/": "scripts/conformance/",
    "kernel/templates/attestation.md": "templates/attestation.md",
    "kernel/templates/precis.md": "templates/precis.md",
    "kernel/templates/INDEX.md": "templates/INDEX.md",
    "kernel/discipline.md": "skills/research-discipline/SKILL.md"
  },
  "_vendored_paths_notes": {
    "kernel/discipline.md": "Upstream ships the discipline body inside skills/research-discipline/SKILL.md (no standalone kernel/discipline.md file). We vendor the six numbered sections BYTE-IDENTICAL; only the wrapper above them is deployment-specific (names our /deep-research + /research-program orchestrators and our schema path). Re-sync = re-copy the six sections verbatim, leave the wrapper."
  },
  "adapted_not_vendored": {
    "skills/adversarial-reader/SKILL.md": "ADAPTED (not byte-vendored) from ARD's adversarial-reader dispatch reference + CATALOGS §4 job catalog (a-h), v0.4.1. It's an ORCHESTRATOR-tier artifact upstream (research-orchestrator/references/), not a kernel file, so we adapt the dispatch framing to our producers while keeping the a-h job catalog faithful. No verbatim hash guard (unlike research-discipline) — track ARD's CATALOGS §4 on re-sync but adaptation is expected."
  },
  "not_yet_vendored": {
    "kernel/templates/dispatch.md": "NOT adopted as a file. dispatch.md is ARD's 9-field registration-declaration template for the research-orchestrator, which Option A does not adopt (we keep our own orchestration). Our /deep-research + /research-program carry their own decomposition/dispatch logic; the discipline-propagation requirement (ARD SPEC §5) is met by inlining research-discipline into specialist dispatch prompts, not by adopting the registration template."
  },
  "conformance": "python3 plugins/research-pipeline/scripts/conformance/run.py"
}
